#include<bits/stdc++.h>
using std::cin,std::cout,std::ios;
constexpr int maxn=1e6;
std::vector<std::string > a;
int n;
std::unordered_map<std::string,int > m;
void solve1(){
    if(m.find(a[0].substr()))
}
int main(){
    ios::sync_with_stdio(false);
    std::string t;
    cin>>n;
    for(int i{1};i<=n;i++){
        cin>>t;
        a.push_back(t);
    }
    if(n==1){

    }
    return 0;
}